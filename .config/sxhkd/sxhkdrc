#Shortcuts
alt + b
	firefox
alt + shift + b
	firefox --private-window
alt + p
	rofi -show run
alt + shift + Return
	st
alt + u
	pmenu --type
alt + shift + u
	pmenu otp --type
alt + shift + c
	wmctrl -c :ACTIVE:
